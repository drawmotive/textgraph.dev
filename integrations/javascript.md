# JavaScript / Node.js

<!--
  CONTENT TO ADD:
  - Install: `npm install textgraph`
  - Basic usage: `import { render } from 'textgraph'`
  - render() API: input string → SVG/HTML string
  - Options object: theme, layout, format, width/height
  - Async usage and streaming (if applicable)
  - TypeScript types
  - Example: generate a diagram server-side with Express and return as PNG
-->
