# CLI

<!--
  CONTENT TO ADD:
  - Install: `npm install -g textgraph`
  - Core commands: render, export, watch, serve
  - Full flags reference for each command
  - Usage examples: render to SVG, watch mode for drafting, export to PDF
  - Exit codes and error messages
  - Shell completion setup (bash/zsh/fish)
-->
