# Getting Started

<!--
  CONTENT TO ADD:
  - Installation options (CLI via npm/pnpm/yarn, VS Code extension, web playground — no install needed)
  - Quick install snippet: `npm install -g textgraph` (or whatever the actual command is)
  - System requirements (Node version, supported OS)
  - Verifying the install: `textgraph --version`
  - Link to "Hello World" as the next step
-->
