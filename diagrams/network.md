# Network / Infrastructure Diagrams

<!--
  CONTENT TO ADD:
  - Using built-in icons for cloud / network components (server, load balancer, DB, firewall, etc.)
  - Zone / VPC grouping
  - Connection protocols as edge labels
  - AWS / GCP / Azure icon packs (if available)
  - Full example: a 3-tier web architecture (client → LB → app servers → DB)
  - Tips for keeping large infra diagrams legible
-->
