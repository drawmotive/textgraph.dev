# Built-in Themes

<!--
  CONTENT TO ADD:
  - Gallery of all built-in themes with a representative rendered diagram for each
  - Theme name tokens (exact strings to use in `theme:` attribute)
  - Color palette tables per theme (primary, secondary, background, text, edge colors)
  - Dark variants of each theme
  - How to extend a built-in theme with overrides
-->
