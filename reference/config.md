# Configuration Options

<!--
  CONTENT TO ADD:
  - textgraph.config.json / textgraph.config.ts file schema
  - All top-level config keys with type, default, and description
  - Project-level vs file-level vs inline (attribute) precedence rules
  - Environment variable overrides (e.g., TEXTGRAPH_THEME)
  - Example config file for a documentation project
  - Example config file for a CI export pipeline
-->
