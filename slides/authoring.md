# Authoring Slides

<!--
  CONTENT TO ADD:
  - How a TextGraph file becomes a slide deck (the `slides` top-level block or file mode)
  - Slide separator syntax (--- or `slide:`)
  - Embedding Markdown content inside a slide: headings, bullets, code blocks, images
  - Embedding a diagram inside a slide (diagram-in-slide)
  - Front matter per slide: title, layout, background
  - Full example: a 5-slide project pitch deck
-->
