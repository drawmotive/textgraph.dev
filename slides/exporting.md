# Exporting Slides

<!--
  CONTENT TO ADD:
  - Export formats: PDF, PNG (per slide), HTML (self-contained), PPTX (if supported)
  - CLI export command: `textgraph export deck.tg --format pdf`
  - Resolution and quality settings
  - Embedding fonts for offline use
  - Automating exports in CI/CD pipelines
-->
